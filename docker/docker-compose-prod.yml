services:
  backend:
    image: ${IMAGE_FULL_URL}
    container_name: ${DOCKERHUB_IMAGE_NAME}
    restart: always
    environment:
      - TZ=Asia/Seoul
      - SERVER_PORT=${SERVER_PORT:-8080}
    network_mode: host
    env_file:
      - ~/.env
